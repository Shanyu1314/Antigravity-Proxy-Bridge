# GitHub Release 发布指南

本文档说明如何发布新版本到 GitHub Release。

## 📋 发布流程

### 步骤 1：提交所有更改

```bash
cd Antigravity-Proxy-Bridge

# 查看更改
git status

# 添加所有更改
git add .

# 提交
git commit -m "v1.1.0: 工业级增强 - 自动探测、文件占用处理、端口预检、哈希审计"
```

### 步骤 2：推送到 GitHub

```bash
# 推送到 main 分支
git push origin main
```

### 步骤 3：创建 Git 标签

```bash
# 创建带注释的标签
git tag -a v1.1.0 -m "Release v1.1.0: Industrial-grade enhancements

新功能：
- 智能场景自动探测
- 增强的端口联通性预检
- 文件哈希审计日志

改进：
- 解决文件占用冲突
- 优化恢复逻辑
- 增强错误提示

详见 CHANGELOG.md"

# 推送标签到 GitHub
git push origin v1.1.0
```

### 步骤 4：在 GitHub 创建 Release

#### 方法 1：通过 GitHub 网页（推荐）

1. 访问仓库页面：https://github.com/Shanyu1314/Antigravity-Proxy-Bridge

2. 点击右侧的 **"Releases"**

3. 点击 **"Draft a new release"** 或 **"Create a new release"**

4. 填写 Release 信息：

   **Tag version**: 选择 `v1.1.0`（刚才推送的标签）
   
   **Release title**: `v1.1.0 - 工业级增强`
   
   **Describe this release**:
   ```markdown
   ## 🚀 v1.1.0 - 工业级增强
   
   ### ✨ 新功能
   
   - **智能场景自动探测**：优先全自动探测 Antigravity Server 安装
   - **端口联通性预检**：安装前检测代理端口是否开启
   - **文件哈希审计**：记录 SHA256 哈希值便于版本比对
   
   ### 🔧 改进
   
   - 解决文件占用冲突（避免 "Text file busy" 错误）
   - 优化恢复逻辑（支持运行中恢复）
   - 增强错误提示和解决方案
   
   ### 📖 完整更新日志
   
   查看 [CHANGELOG.md](https://github.com/Shanyu1314/Antigravity-Proxy-Bridge/blob/main/CHANGELOG.md)
   
   ### 📥 安装
   
   ```bash
   git clone https://github.com/Shanyu1314/Antigravity-Proxy-Bridge.git
   cd Antigravity-Proxy-Bridge
   sudo ./setup.sh
   ```
   
   ### 🔄 从 v1.0.0 升级
   
   ```bash
   cd Antigravity-Proxy-Bridge
   git pull
   sudo ./uninstall.sh  # 卸载旧版本
   sudo ./setup.sh      # 安装新版本
   ```
   ```

5. **可选**：上传附件（如果需要）
   - 可以上传打包的 zip 文件
   - GitHub 会自动生成 Source code (zip) 和 Source code (tar.gz)

6. **Set as the latest release**：勾选（设为最新版本）

7. 点击 **"Publish release"**

#### 方法 2：使用 GitHub CLI（高级）

```bash
# 安装 GitHub CLI (如果还没有)
# Windows: winget install GitHub.cli
# 或访问: https://cli.github.com/

# 登录
gh auth login

# 创建 Release
gh release create v1.1.0 \
  --title "v1.1.0 - 工业级增强" \
  --notes-file CHANGELOG.md \
  --latest
```

## 📦 版本号规范

遵循 [语义化版本](https://semver.org/lang/zh-CN/)：

- **v1.0.0** → **v1.1.0**：新增功能（向后兼容）
- **v1.1.0** → **v1.1.1**：Bug 修复（向后兼容）
- **v1.1.0** → **v2.0.0**：破坏性变更（不兼容）

## 🔍 验证 Release

发布后验证：

1. 访问 Releases 页面确认新版本
2. 下载 Source code 测试
3. 检查标签是否正确
4. 验证 Release notes 显示正常

## 📝 Release Notes 模板

```markdown
## 🚀 v{版本号} - {版本名称}

### ✨ 新功能
- 功能 1
- 功能 2

### 🔧 改进
- 改进 1
- 改进 2

### 🐛 修复
- 修复 1
- 修复 2

### 📖 文档
- 文档更新

### ⚠️ 破坏性变更
- 变更说明（如果有）

### 🔄 升级指南
```bash
升级命令
```

### 📥 安装
```bash
安装命令
```
```

## 🎯 最佳实践

1. **先测试再发布**：在测试环境验证所有功能
2. **更新 CHANGELOG**：详细记录所有变更
3. **语义化版本**：遵循版本号规范
4. **清晰的 Release Notes**：让用户快速了解变更
5. **标签和 Release 同步**：确保标签和 Release 版本一致

## 🔄 管理多个版本

### 保留旧版本

- GitHub Release 会自动保留所有历史版本
- 用户可以下载任意版本的 Source code
- 旧版本的标签会一直存在

### 用户如何使用特定版本

```bash
# 克隆特定版本
git clone --branch v1.0.0 https://github.com/Shanyu1314/Antigravity-Proxy-Bridge.git

# 或者切换到特定版本
git checkout v1.0.0
```

### 标记为 Pre-release

如果是测试版本：
1. 在创建 Release 时勾选 **"Set as a pre-release"**
2. 版本号使用 `v1.1.0-beta.1` 格式

## 📊 Release 统计

GitHub 会自动统计：
- 下载次数
- Star 数量
- Fork 数量
- 访问量

## 🆘 常见问题

### Q: 如何删除错误的 Release？

A: 在 Release 页面点击 Release，然后点击 "Delete" 按钮

### Q: 如何编辑已发布的 Release？

A: 在 Release 页面点击 "Edit release" 按钮

### Q: 标签推送错了怎么办？

```bash
# 删除本地标签
git tag -d v1.1.0

# 删除远程标签
git push origin :refs/tags/v1.1.0

# 重新创建并推送
git tag -a v1.1.0 -m "新的标签信息"
git push origin v1.1.0
```

### Q: 如何设置默认分支？

在仓库 Settings → Branches → Default branch 中设置

---

**现在你可以发布 v1.1.0 了！** 🎉
